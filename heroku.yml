build:
  docker:
    backend: ./rails-nextjs-demo-backend/Dockerfile
    frontend: ./rails-nextjs-demo-frontend/Dockerfile
    web: ./nginx/Dockerfile

# release:
#   image: backend
#   command:
#     - bundle exec rails db:migrate 2>/dev/null || bundle exec rails db:create db:migrate db:seed

run:
  backend: bundle exec rails s -p 8080 -b '0.0.0.0'
  frontend: yarn build && yarn start -p 3000
